import { PageWrapper } from "@/components/page-wrapper";
import { SkillsSection } from "@/sections/skills";

export const metadata = { title: "Skills — Naman Mehra" };

export default function SkillsPage() {
  return (
    <PageWrapper>
      <SkillsSection />
    </PageWrapper>
  );
}
