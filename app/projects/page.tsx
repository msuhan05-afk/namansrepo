import { PageWrapper } from "@/components/page-wrapper";
import { ProjectsSection } from "@/sections/projects";

export const metadata = { title: "Work — Naman Mehra" };

export default function ProjectsPage() {
  return (
    <PageWrapper>
      <ProjectsSection />
    </PageWrapper>
  );
}
