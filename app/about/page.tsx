import { PageWrapper } from "@/components/page-wrapper";
import { AboutSection } from "@/sections/about";

export const metadata = { title: "About — Naman Mehra" };

export default function AboutPage() {
  return (
    <PageWrapper>
      <AboutSection />
    </PageWrapper>
  );
}
