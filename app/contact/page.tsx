import { PageWrapper } from "@/components/page-wrapper";
import { ContactSection } from "@/sections/contact";

export const metadata = { title: "Contact — Naman Mehra" };

export default function ContactPage() {
  return (
    <PageWrapper>
      <ContactSection />
    </PageWrapper>
  );
}
